# build intermediates (the c2go pipeline)
*.bc
*.o
*.tmp
/build/
/cmake-build-*/

# musl is a LOCAL symlink (-> ../musl) for now; becomes a git submodule at push time.
/musl

# NOTE: the shipped module = generated *.go/*.s + hand-written *.go at the REPO ROOT;
# these ARE committed. csrc/ gets its own go.mod (nested module) so its *.c stay out of
# the module zip (they are build inputs, not shipped artifacts).

# local / editor / tooling
.codex-gpt-session-id
.DS_Store

# never track local portability notes (see LICENSING_PLAN_A)
PORTABILITY.md
